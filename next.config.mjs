/** @type {import('next').NextConfig} */
const nextConfig = {
    // Configuración existente (si tienes más configuraciones, mantenlas aquí)
    
    // Agrega la configuración de dominios permitidos para imágenes
    images: {
      domains: ['images.unsplash.com'],
    },
  };
  
  export default nextConfig;
  